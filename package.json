{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "debug": "node --inspect app.js",
    "build": "npm run build:clean && npm run build:deps",
    "build:clean": "rm -rf node_modules package-lock.json && npm install",
    "build:deps": "npm ci --only=production",
    "build:optimize": "npm prune --production"
  },
  "author": "Fazul Imam",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "ejs": "^3.1.8",
    "express": "^4.18.1",
    "express-session": "^1.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
